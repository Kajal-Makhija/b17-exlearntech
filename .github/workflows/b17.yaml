name: b17-workflow

on: push

jobs:
    first:
      runs-on: [self-hosted, Linux, prod]

      steps:
        - name: Create a file and view
          run: | 
            echo "This is test reports" > report.txt
            cat report.txt

        # - name: Upload the file
        #   uses: actions/upload-artifact@v7
        #   with:
        #     name: report.txt
        #     path: ./report.txt

    # second:
    #     runs-on: ubuntu-latest
    #     steps:

    #       - name: Download Artifacts
    #         uses: actions/download-artifact@v4
    #         with:
    #           name: report.txt

    #       - name: View the reports
    #         run: cat report.txt
#   first:
#     runs-on: ubuntu-latest
#     steps:
#       - name: print message
#         run: |
#             sleep 10 
#             echo "Hello world!!!!"
#   second:
#     runs-on: ubuntu-22.04-arm
#     needs: first
#     steps:
#       - name: Print message for second job
#         run: |
#             sleep 10
#             echo "This is my second job"